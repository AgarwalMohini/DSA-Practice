from os import *
from sys import *
from collections import *
from math import *

def rowWaveForm(mat):
    # Write your code here.
    l=[]
    for i in range(len(mat)):
        if i%2==0:
            l.extend(mat[i])
        else:
            l.extend(reversed(mat[i]))
    return l
    pass
