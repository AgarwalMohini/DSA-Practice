from typing import List

def minDiff(n: int, arr: List[int]) -> int:
    if n < 2:
        return 0  

    arr.sort()
    mini = float('inf')
    for i in range(1, n):
        mini = min(mini, arr[i] - arr[i-1])
    return mini
